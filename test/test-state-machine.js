require('should')

describe('Statemachine' , function() {
    describe('#pipeEvent', function() {
        it('should send an event to the current state', function() {
            (true).should.equal(true)
        })
    })
})